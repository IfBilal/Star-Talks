import { useEffect, useState } from 'react';
import { router, useLocalSearchParams } from 'expo-router';
import { ActivityIndicator, Text, View } from 'react-native';
import { Colors } from '@/constants/theme';
import { requireSupabase } from '@/lib/supabase';

export default function AuthCallback(){const params=useLocalSearchParams<{code?:string;token_hash?:string;type?:string}>();const [error,setError]=useState('');useEffect(()=>{let alive=true;void(async()=>{try{const db=requireSupabase();if(typeof params.code==='string'){const {error}=await db.auth.exchangeCodeForSession(params.code);if(error)throw error}else if(typeof params.token_hash==='string'&&typeof params.type==='string'){const allowed=['signup','email','magiclink','recovery','invite','email_change'] as const;if(allowed.includes(params.type as typeof allowed[number])){const {error}=await db.auth.verifyOtp({token_hash:params.token_hash,type:params.type as typeof allowed[number]});if(error)throw error}}const {data:{user}}=await db.auth.getUser();if(!user)throw new Error('Sign-in did not complete. Return to the app and try again.');const {data}=await db.from('birth_profiles').select('id').eq('user_id',user.id).eq('relationship','self').maybeSingle();if(alive)router.replace(data?'/home':'/profile')}catch(e){if(alive)setError(e instanceof Error?e.message:'Sign-in could not be completed.')}})();return()=>{alive=false}},[params.code,params.token_hash,params.type]);return <View style={{flex:1,backgroundColor:Colors.ivory,alignItems:'center',justifyContent:'center',padding:28}}>{error?<Text style={{fontFamily:'Poppins_400Regular',fontSize:13,color:Colors.danger,textAlign:'center'}}>{error}</Text>:<><ActivityIndicator color={Colors.indigo}/><Text style={{fontFamily:'Poppins_500Medium',fontSize:13,color:Colors.indigo,marginTop:12}}>Signing you in to Star Talks…</Text></>}</View>}
