import { useState } from 'react';
import { router } from 'expo-router';
import { Camera, ChevronDown } from 'lucide-react-native';
import { Modal, Pressable, Text, View } from 'react-native';
import { PrimaryButton, Screen, ScreenDecoration, TextField, Title } from '@/components/brand';
import { Colors } from '@/constants/theme';
import { requireSupabase } from '@/lib/supabase';
import { preferences, readRegion } from '@/lib/preferences';
import { useEffect } from 'react';

export default function ProfileScreen(){const [name,setName]=useState('');const [gender,setGender]=useState('Female');const [genderOpen,setGenderOpen]=useState(false);const [busy,setBusy]=useState(false);const [error,setError]=useState('');useEffect(()=>{void(async()=>{try{const user=(await requireSupabase().auth.getUser()).data.user;if(!user)return;const {data}=await requireSupabase().from('profiles').select('display_name,gender').eq('id',user.id).maybeSingle();if(data?.display_name)setName(data.display_name);if(data?.gender&&['female','male','other'].includes(data.gender.toLowerCase()))setGender(data.gender[0].toUpperCase()+data.gender.slice(1).toLowerCase())}catch{}})()},[]);const save=async()=>{if(!name.trim()){setError('Enter your name to continue.');return}setBusy(true);try{const db=requireSupabase();const user=(await db.auth.getUser()).data.user;if(!user)throw new Error('Please sign in to save your profile.');const rawRegion=await preferences.getRegion();const region=readRegion(rawRegion);const language=await preferences.getLanguage()??'English';const languageCode=language==='हिन्दी'?'hi':language==='اردو'?'ur':language==='Español'?'es':language==='Français'?'fr':language==='العربية'?'ar':'en';const {error}=await db.from('profiles').upsert({id:user.id,display_name:name.trim(),gender:gender.toLowerCase(),country_code:region?.code,country_name:region?.name,currency_code:region?.currency,date_format:region?.dateFormat,language_code:languageCode},{onConflict:'id'});if(error)throw error;router.push('/birth-details')}catch(e){setError(e instanceof Error?e.message:'Could not save profile.');}finally{setBusy(false)}};return <Screen style={{paddingTop:30}}><View style={{flex:1,zIndex:1}}><Title subtitle="Let's get to know you better">Create Your Profile</Title><Pressable style={{alignSelf:'center',width:74,height:74,borderRadius:50,backgroundColor:'#ECE9F9',alignItems:'center',justifyContent:'center',marginBottom:20}}><Camera color={Colors.indigo} size={25}/></Pressable><TextField label="Full Name" value={name} onChangeText={setName} placeholder="Your full name"/><Text style={{fontFamily:'Poppins_500Medium',fontSize:12,color:Colors.text,marginBottom:6,marginTop:3}}>Gender</Text><Pressable onPress={()=>setGenderOpen(true)} style={{height:43,borderRadius:8,borderWidth:1,borderColor:'#E5E3DF',backgroundColor:'white',paddingHorizontal:12,alignItems:'center',justifyContent:'space-between',flexDirection:'row'}}><Text style={{fontFamily:'Poppins_400Regular',fontSize:12,color:Colors.text}}>{gender}</Text><ChevronDown color={Colors.muted} size={17}/></Pressable>{error?<Text style={{color:Colors.danger,fontSize:11,marginTop:8}}>{error}</Text>:null}<View style={{flex:1}}/><PrimaryButton title="Continue" loading={busy} onPress={save}/></View><ScreenDecoration/><Modal visible={genderOpen} animationType="fade" transparent onRequestClose={()=>setGenderOpen(false)}><Pressable onPress={()=>setGenderOpen(false)} style={{flex:1,backgroundColor:'#0005',justifyContent:'center',paddingHorizontal:35}}><View style={{backgroundColor:'white',borderRadius:14,padding:8}}>{['Female','Male','Other'].map(g=><Pressable key={g} onPress={()=>{setGender(g);setGenderOpen(false)}} style={{height:48,justifyContent:'center',paddingHorizontal:12,borderBottomWidth:.5,borderBottomColor:'#EEE'}}><Text style={{fontFamily:'Poppins_400Regular',fontSize:13,color:Colors.text}}>{g}</Text></Pressable>)}</View></Pressable></Modal></Screen>}
